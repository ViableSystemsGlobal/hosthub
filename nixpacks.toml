[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm ci --legacy-peer-deps"]

[phases.build]
cmds = [
  # Build command includes prisma generate now
  "npm run build",
  # Copy static files into standalone directory (Next.js doesn't include them)
  "cp -r .next/static .next/standalone/.next/static",
  # Copy public folder into standalone
  "cp -r public .next/standalone/public"
]

[start]
# Run with PORT 3000 and HOSTNAME 0.0.0.0
cmd = "PORT=3000 HOSTNAME=0.0.0.0 node .next/standalone/server.js"

